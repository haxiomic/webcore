main.hxml

--library hxcpp
--cpp platform/ios/_hxcpp-bin

# Leaving SDK empty should map to the default
# if you have errors about missing SDK then explicitly set the SDK version on your system (e.g. -D IPHONE_VER=12.2)
-D IPHONE_VER=

# Compile for ios simulator by default
# The generated Xcode project will compile for iphoneos when a real device is targeted
-D iphonesim
-D HXCPP_M64